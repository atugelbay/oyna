# Скопировать в /var/www/oyna/frontend/.env.production на сервере (не коммитить).
# Файл должен существовать ДО npm run build — иначе NEXT_PUBLIC_* не попадут в бандл.

NEXT_PUBLIC_API_URL=https://api.example.com/api
